function Navbar() {
  return (
    <nav>
      <h1>📚 Library Management System</h1>

      <div>
        <button>Login</button>
        <button>Register</button>
        <button>Books</button>
      </div>
    </nav>
  );
}

export default Navbar;